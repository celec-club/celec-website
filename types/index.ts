export type eventsProps = {
  title: string;
  description: string;
  image: string;
};

export type Links = {
  name: string;
  path: string;
};

export type SProps = {
  name: string;
  logo: string;
}